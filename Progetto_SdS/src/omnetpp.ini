[General]
network = Network
config-recording = all
repeat = 20
**.scalar-recording = true

**.S1.interArrivalTime =exponential(${1.0, 0.5, 0.25, 0.1, 0.05}s) 
**.S2.interArrivalTime =exponential(${0.5, 0.25}s) 

**.servente.serviceTime = uniform(0.11s,0.55s)
**.servente.serviceTime_2 = uniform(0.1s,0.4s)
**.servente.fetchingAlgorithm = "threshold"


**.first.Threshold = 10
**.second.Threshold = 3

**.first.capacity = 10
**.second.capacity = -1 
**.servente.switchOverTime = uniform(0.1s,0.3s)
